# VerumDot — dependency manifest
#
# This file documents what ./setup.sh installs. The installer is the source of
# truth (see pkgs_for / aur_for in setup.sh); groups here mirror the ones the
# wizard offers. Core is always installed, every other group is a checkbox.

# ── core (always) ──────────────────────────────────────────────────────
hyprland
hyprpaper
hypridle
hyprlock
hyprcursor
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
waybar
rofi
mako
jq
inotify-tools
python
curl
rsync

# ── audio ──────────────────────────────────────────────────────────────
pipewire
pipewire-pulse
pipewire-alsa
wireplumber
pamixer
playerctl

# ── capture ────────────────────────────────────────────────────────────
grim
slurp
swappy
wl-clipboard

# ── network + bluetooth ────────────────────────────────────────────────
networkmanager
network-manager-applet
bluez
bluez-utils
blueman

# ── power / brightness ─────────────────────────────────────────────────
brightnessctl
power-profiles-daemon
upower

# ── theming (PureBlackGlass targets) ───────────────────────────────────
kvantum
qt6ct

# ── fonts ──────────────────────────────────────────────────────────────
ttf-jetbrains-mono-nerd
ttf-iosevka-nerd
ttf-nerd-fonts-symbols
noto-fonts
noto-fonts-emoji

# ── desktop apps (the wizard lets you swap each one) ───────────────────
# terminal:     kitty | alacritty | foot | wezterm | ghostty
# browser:      firefox | chromium | brave-bin (AUR) | zen-browser-bin (AUR)
# file manager: nautilus | thunar | dolphin | nemo
# editor:       visual-studio-code-bin (AUR) | neovim | zed
kitty
firefox
nautilus

# ── AUR (yay / paru; setup.sh can bootstrap yay-bin) ───────────────────
spotify
kora-icon-theme
qt5ct
visual-studio-code-bin

# ── optional, off by default ───────────────────────────────────────────
# eww   legacy volume/power/calendar panels; rofi versions replaced them
