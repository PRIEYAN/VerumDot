# Basic Hyprlock starter config
# THEME: use these color placeholders to retheme later

font = "JetBrains Mono 20"
time_font = "JetBrains Mono 48"
date_font = "JetBrains Mono 18"
clock = true
blur = false

background = rgba(0, 0, 0, 0.85) # THEME: background
text_color = rgba(255, 255, 255, 0.95) # THEME: text
indicator_color = rgba(136, 192, 208, 0.90) # THEME: indicator
failed_attempt_color = rgba(220, 80, 80, 0.90) # THEME: wrong input
