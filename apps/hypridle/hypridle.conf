general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances
    before_sleep_cmd = loginctl lock-session    # lock before sleep
    after_sleep_cmd = hyprctl dispatch dpms on  # turn on display after sleep to avoid double-key presses
}

listener {
    timeout = 300                                 # 5 minutes
    on-timeout = loginctl lock-session            # lock screen when timeout has passed
}

listener {
    timeout = 330                                 # 5.5 minutes
    on-timeout = hyprctl dispatch dpms off        # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on          # screen on when activity is detected
}

# Long idle: suspend now, then logind's HibernateDelaySec flips it to
# hibernate so a forgotten, unplugged laptop saves its session instead of
# draining flat. Lid-close is handled by systemd-logind (same action),
# not here. Requires the swapfile + resume setup to be in place.
listener {
    timeout = 1800                                # 30 minutes
    on-timeout = systemctl suspend-then-hibernate
}
